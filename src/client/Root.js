import React from 'react';
import { HashRouter } from 'react-router-dom';
import App from './../shared/App.js';

const Root = () => (
    <HashRouter>
        <App/>
    </HashRouter>
);

export default Root;
